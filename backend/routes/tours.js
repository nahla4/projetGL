// pour la table tours + tourphotos + tourhighlights + tourincluded + tourexcluded
//CRUD de la table tours.

//Inclut ou joint tourphotos, tourhighlights, tourincluded, tourexcluded pour renvoyer une fiche tour complète.

//Endpoints typiques :

//POST /api/tours (création d’un tour par un guide, avec calcul du prix à partir de sa grille tarifaire).

//GET /api/tours (annuaire des tours, filtres par ville/wilaya via cities).

//GET /api/tours/:id (détail d’un tour).