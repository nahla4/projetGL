// pour la table customrequests (tours sur mesure)
//customRequests.js

//Pour les demandes de “tours sur mesure” (table customrequests).

//Endpoints :

////POST /api/custom-requests (touriste décrit ce qu’il veut).

//GET /api/custom-requests/me (pour le touriste).

//GET /api/custom-requests/guide (pour le guide).

